import React from "react"

import { Body } from "../../../styles/style"

const Iterations = (props): JSX.Element => {
	return (
		<Body>
			<p> Event Iterations </p>
		</Body>
	)
}

export default Iterations
