import React from 'react'
import styled from 'styled-components'

import { Body } from '../../../styles/style'

const Feedback = prop => {
  return (
    <div>
      <Body> Event Feedback forms </Body>
    </div>
  )
}

export default Feedback
